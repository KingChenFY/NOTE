# C

## board_server_get_use_data

### 原型
`
WK_INT32 board_server_get_use_data( WK_OUT WK_UINT32* puDataLength, WK_OUT WK_UINT8* puType, WK_OUT WK_POINT* puDataPoint, WK_OUT WK_UINT32*puDataIndex )
`

### 函数功能
从接收队列中获取接收的数据，数据来源 ***s_stboard_server_list*** 

### 函数参数
`puDataPoint`：从接收队列中获得的数据存放地址
`puType`：
`puDataLength`：接收数据的长度
`puDataIndex`：

### 返回
`WK_OK`
`WK_ERR`

# ActionDManage

## checkBoardCmdReturn

### 原型
`WK_BOOL ActionDManage::checkBoardCmdReturn(WK_IN WK_UINT64 curTm, WK_OUT emBoardCmdId& eCmdId, WK_OUT WK_UINT8** ppuCmdData)`

### 函数功能
根据调用 *`board_server_get_use_data`* 函数获得的 `puType` 指令类型，处理 `puDataPoint` 数据。

### 函数参数
`curTm`：
`eCmdId`：接收数据的指令ID
`ppuCmdData`：从指令ID位开始的数据，到数据尾


### 返回
`WK_TRUE`
`WK_FALSE`

# ThdActionCGroup

## checkTask

### 原型
`WK_BOOL ThdActionCGroup::checkTask( WK_UINT64 curTm )`

### 函数功能
从 **m_poolTaskC** C层任务池中获取任务，并依据C层任务类型解析并设置任务信息`m_stTask`和动作信息`m_eActionType`。

### 函数参数
`curTm`：

### 返回
`WK_TRUE`
`WK_FALSE`

## doAction

### 原型
`WK_BOOL ThdActionCGroup::doAction ( WK_UINT64 curTm )`

### 函数功能
根据 ***`checkTask`*** 设置的任务信息，调用对应的Action函数。任务下发到发送队列在对应的Action中实现。

### 函数参数
`curTm`：

### 返回
`WK_TRUE`
`WK_FALSE`



 